# The hook imports bashpolicy on every Bash call, so Python writes
# bytecode next to the package. Keeping it speeds hook startup; it just
# does not belong in the repo.
__pycache__/
*.py[cod]

# Claude Code worktrees live inside the repo; they are checkouts of it,
# never content of it.
.claude/worktrees/
