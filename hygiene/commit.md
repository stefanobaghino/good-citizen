# Commit messages

- Subject line is imperative mood, ≤ ~70 characters, no trailing period.
- **Drop trailers** (`Co-Authored-By`, `Signed-off-by`, etc.) unless the repo's contribution guide requires them.
